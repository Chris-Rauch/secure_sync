//import 'package:logging/logging.dart';

/*
final Logger logger = Logger('AppLogger');
bool kDebugMode = true;

void setupLogging() {
  // Set the logging level (can be Level.INFO, Level.WARNING, Level.SEVERE, etc.)
  logger.level = Level.ALL;

  // Add a listener for logging messages
  logger.onRecord.listen((LogRecord record) {
    if (kDebugMode) {
        // ignore: avoid_print
        print('${record.level.name}: ${record.time}: ${record.message}');
      
    }
  });
}
*/